{% extends 'base.html.twig' %}

{% block title %}Typo2{% endblock %}

{% block body %}
    <h1>Typo2</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typo2.id }}</td>
            </tr>
            <tr>
                <th>Lib_typo2</th>
                <td>{{ typo2.libTypo2 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('typo2_index') }}">back to list</a>

    <a href="{{ path('typo2_edit', {'id': typo2.id}) }}">edit</a>

    {{ include('typo2/_delete_form.html.twig') }}
{% endblock %}
