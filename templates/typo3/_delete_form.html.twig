<form method="post" action="{{ path('typo3_delete', {'id': typo3.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typo3.id) }}">
    <button class="btn">Delete</button>
</form>
