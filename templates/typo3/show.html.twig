{% extends 'base.html.twig' %}

{% block title %}Typo3{% endblock %}

{% block body %}
    <h1>Typo3</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typo3.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('typo3_index') }}">back to list</a>

    <a href="{{ path('typo3_edit', {'id': typo3.id}) }}">edit</a>

    {{ include('typo3/_delete_form.html.twig') }}
{% endblock %}
