<form method="post" action="{{ path('typo1_delete', {'id': typo1.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typo1.id) }}">
    <button class="btn">Delete</button>
</form>
