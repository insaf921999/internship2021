{% extends 'base.html.twig' %}

{% block title %}Typo1 index{% endblock %}

{% block body %}
    <h1>Typo1 index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Lib_typo1</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for typo1 in typo1s %}
            <tr>
                <td>{{ typo1.id }}</td>
                <td>{{ typo1.libTypo1 }}</td>
                <td>
                    <a href="{{ path('typo1_show', {'id': typo1.id}) }}">show</a>
                    <a href="{{ path('typo1_edit', {'id': typo1.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('typo1_new') }}">Create new</a>
{% endblock %}
