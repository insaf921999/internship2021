{% extends 'base.html.twig' %}

{% block title %}Typo1{% endblock %}

{% block body %}
    <h1>Typo1</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typo1.id }}</td>
            </tr>
            <tr>
                <th>Lib_typo1</th>
                <td>{{ typo1.libTypo1 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('typo1_index') }}">back to list</a>

    <a href="{{ path('typo1_edit', {'id': typo1.id}) }}">edit</a>

    {{ include('typo1/_delete_form.html.twig') }}
{% endblock %}
